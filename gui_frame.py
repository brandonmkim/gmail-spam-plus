from tkinter import *

# stores basic gui frames separately to be accessed by both main and emailClass
root = Tk()
root.title("gee mail")
content = Frame(root, width=1000, height=600)

maxCol = 4
maxRow = 5